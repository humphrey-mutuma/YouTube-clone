import "./RecommendedVideosShortcut.css"

const RecommendedVideosShortcut = ({ title }) => {
  return (
    <div className="RecommendedVideosShortcut">
      <span>{title}</span>
    </div>
  );
};

export default RecommendedVideosShortcut;
